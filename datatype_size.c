#include<stdio.h>
int main()
{
        int a;
        char b;
        int b_size;
        b_size=sizeof(b);
        printf("\nsize of integer = %d",sizeof(a));
        printf("\nsize of character = %d",b_size);
        printf("\nsize of double = %d",sizeof(double));
        return 0;        
}
